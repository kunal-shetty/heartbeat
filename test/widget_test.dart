// Chatterbox — basic smoke test
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full widget tests require Supabase + Firebase to be initialised.
    // Use integration tests for end-to-end coverage.
    expect(true, isTrue);
  });
}